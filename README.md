# Lancer le client

``` bash
cd client
composer install // ou composer update
php -S localhost:8081
```

# Lancer le serveur

``` bash
cd server
composer install // ou composer update
php -S localhost:8082
```
