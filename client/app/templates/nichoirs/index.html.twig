{% extends 'base/user.html.twig' %}


{% block header %}
    <header>
        <a href="/logout" class="logout">logout</a>
        <div class="lpo_logo"></div>
        <div class="user">
            <div class="photo" style="background-image: url('{{ user.photo }}')"></div>
            <div class="nom">{{ user.nom }} {{ user.prenom }}</div>
            <div class="info">
                <span>login : {{ user.login }}</span><br><br>
                <span>Mail : {{ user.mail }}</span><br><br>
                <span>Tel : {{ user.telephone }}</span><br><br>
                <span>Adresse : {{ user.adresse.rue }} {{ user.adresse.ville }} {{ user.adresse.cp }}</span>
            </div>
        </div>
    </header>
{% endblock %}

{% block body %}

<h1 class="mes_nichoirs">
    <!-- TODO
    ajouter un nichoir
    -->
    <a href="/nichoirs" class="ajouter">Ajouter</a>
    Mes nichoirs</h1>
    <div class="container_nichoirs">
    {% for nichoir in user.nichoirs %}
        <div class="card" style="border: solid black 1px;">
            <!-- TODO
            supprimer le nichoir
            -->
            <a href="/nichoirs/{{nichoir.id}}" class="cross">X</a>
            <div class="photo_nichoir" style="background-image: url('{{nichoir.photo}}')"></div>
            <h1 class="nom_nichoir">{{ nichoir.nom }}</h1>
            <h2 class="date_nichoir">Date d'installation : {{ nichoir.date_installation | date("m/d/Y") }}</h2>
            <p style="clear: both;"></p>
            <h2 class="geoloc">Géolocalisation :<br><br>
            <span>Latitude : {{ nichoir.geolocalisation.latitude }}</span><br>
            <span>Longitude : {{ nichoir.geolocalisation.longitude }}</span>
            </h2>
        </div>
    {% endfor %}
    </div>

{% endblock %}
